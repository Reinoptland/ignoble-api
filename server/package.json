{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for ignoble api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:undo:all": "npx sequelize-cli db:migrate:undo:all",
    "reset": "npm run migrate:undo:all && npm run migrate"
  },
  "keywords": [
    "ignoble",
    "prize"
  ],
  "author": "Rein Op 't Land",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "pg": "^8.5.1",
    "sequelize": "^6.3.5",
    "sequelize-cli": "^6.2.0"
  }
}
